'use strict';

/**
 * credits-calculation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::credits-calculation.credits-calculation');
