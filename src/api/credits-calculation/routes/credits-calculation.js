'use strict';

/**
 * credits-calculation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::credits-calculation.credits-calculation');
