'use strict';

/**
 * credits-calculation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::credits-calculation.credits-calculation');
